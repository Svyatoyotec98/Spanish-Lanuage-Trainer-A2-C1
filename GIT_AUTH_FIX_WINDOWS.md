# Решение проблемы с Git авторизацией на Windows

## ❗ ЕСЛИ ПОЛУЧАЕТЕ ОШИБКУ 403 "Permission denied"

Это значит токен неправильный или не имеет прав. Выполните ВСЕ шаги ниже:

### Шаг 1: Удалите старые credentials

```bash
# В PowerShell выполните:
cmdkey /list | findstr git

# Удалите все git credentials (если есть):
cmdkey /delete:git:https://github.com
cmdkey /delete:LegacyGeneric:target=git:https://github.com
```

### Шаг 2: Создайте НОВЫЙ токен с ПРАВИЛЬНЫМИ правами

1. **Откройте:** https://github.com/settings/tokens
2. **Нажмите:** "Generate new token" → "Generate new token (classic)"
3. **ВАЖНО - выберите ВСЕ эти permissions:**
   - ✅ **repo** (поставьте галочку на repo - это откроет все под-пункты)
     - ✅ repo:status
     - ✅ repo_deployment
     - ✅ public_repo
     - ✅ repo:invite
     - ✅ security_events
   - ✅ **workflow** (если используете GitHub Actions)
4. **Срок действия:** Выберите "No expiration" (без истечения) или длительный срок
5. **Нажмите:** "Generate token"
6. **СКОПИРУЙТЕ токен СРАЗУ** (показывается только один раз!)

### Шаг 3: Настройте Git и сделайте push

**ВАРИАНТ A: Токен в URL (САМЫЙ ПРОСТОЙ)**

```bash
# 1. Удалите старый remote
git remote remove origin

# 2. Добавьте новый remote с токеном в URL
git remote add origin https://ВАШ_ТОКЕН_СЮДА@github.com/Svyatoyotec98/Spanish-Lanuage-Trainer-A2-C1.git

# 3. Проверьте (токен должен быть в URL)
git remote -v

# 4. Теперь push без запроса пароля
git push -u origin claude/fix-git-auth-ytzYE
```

**ВАЖНО:**
- Замените `ВАШ_ТОКЕН_СЮДА` на токен, который вы скопировали
- Токен выглядит примерно так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
- Проверьте, что в URL нет лишних пробелов

---

**ВАРИАНТ B: Credential Store**

```bash
# 1. Сбросьте все credential helpers
git config --global --unset-all credential.helper

# 2. Установите store
git config --global credential.helper store

# 3. Верните HTTPS URL без токена
git remote set-url origin https://github.com/Svyatoyotec98/Spanish-Lanuage-Trainer-A2-C1.git

# 4. Push (введите токен как пароль)
git push

# Когда попросит:
# Username: Svyatoyotec98
# Password: вставьте_ваш_токен_здесь (НЕ пароль от GitHub!)
```

После первого ввода Git сохранит токен в файл `C:\Users\ваш_пользователь\.git-credentials`

---

## 🔍 Диагностика: Если ВСЁЩЕ не работает

### Проверьте, что токен правильный:

1. **Откройте:** https://github.com/settings/tokens
2. **Найдите свой токен** в списке
3. **Проверьте:**
   - ✅ Есть ли галочка **repo**
   - ✅ Не истек ли срок действия
   - ✅ Токен создан для вашего аккаунта (Svyatoyotec98)

### Проверьте текущие настройки Git:

```bash
# Проверьте remote URL
git remote -v

# Проверьте credential helper
git config --list | findstr credential

# Проверьте, какой пользователь в Windows Credential Manager
cmdkey /list | findstr git
```

### Полный сброс (если ничего не помогло):

```bash
# 1. Удалите ВСЕ git credentials из Windows
rundll32.exe keymgr.dll, KRShowKeyMgr
# В открывшемся окне удалите все связанные с git записи

# 2. Удалите файл с сохраненными credentials
del %USERPROFILE%\.git-credentials

# 3. Сбросьте все настройки credential
git config --global --unset-all credential.helper
git config --system --unset-all credential.helper

# 4. Начните с ВАРИАНТА A (токен в URL) из Шага 3
```

---

## ❓ Почему это не работало раньше?

1. **Git Credential Manager (GCM)** на Windows перехватывает авторизацию и открывает браузер
2. Простая настройка `credential.helper store` не работает, потому что GCM имеет приоритет
3. **Решение:** Либо токен в URL, либо полное удаление GCM

---

## ✅ Как проверить, что всё работает:

После настройки:

```bash
# Это должно работать БЕЗ запроса пароля
git push

# Вы должны увидеть:
# Everything up-to-date
# или
# To https://github.com/Svyatoyotec98/Spanish-Lanuage-Trainer-A2-C1.git
#    xxxxx..xxxxx  branch_name -> branch_name
```

Если видите это - **ПОЗДРАВЛЯЮ, РАБОТАЕТ!** 🎉
